#include <stdio.h>
int main()
{
    int n, type;
    scanf("%d %d", &n, &type);
    if (type == 1)
    {
        for (int i = 0; i < length; i++)
        {
            
        }
    }
    else if (type == 2)
    {
        
    }
    else if (type == 3)
    {

    }
}